# Auto-DNS
Auto DNS is basically a batch file that checks for the best dns froma list of popular dns and applies that directly to your wifi adapter properties so that you don't have to do that manually.
